name: cloud-edge
version: 0.0.1
apiVersion: 0
splitRepo: sfi-domain-cloud-edge
status: scaffold
description: Edge runtime, fleet orchestration, and cloud analytics platform.

capabilities:
  - edge.runtime
  - fleet.orchestrate
  - tenant.isolation
  - analytics.batch

dependencies:
  core:
    - contracts
    - core-bus
    - plugin-host
  optional:
    - hal-rs
    - math-kernel
  plugins:
    - ai-infer

profiles:
  default: profiles/edge-agent.yaml
