name: bio-medical
version: 0.0.1
apiVersion: 0
splitRepo: sfi-domain-biomed
status: scaffold
description: Sequencing, genomic analysis, and medical imaging pipelines.

capabilities:
  - instrument.sequencer
  - imaging.process
  - genomics.align
  - audit.clinical

dependencies:
  core:
    - contracts
    - hal-rs
    - core-bus
    - plugin-host
  optional:
    - math-kernel
  plugins:
    - ai-infer

profiles:
  default: profiles/clinical-imaging.yaml
