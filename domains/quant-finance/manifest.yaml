name: quant-finance
version: 0.0.1
apiVersion: 0
splitRepo: sfi-domain-quant
status: scaffold
description: Low-latency trading, risk, and Julia-powered research backtesting.

capabilities:
  - market.data
  - trading.execute
  - risk.gate
  - research.backtest

dependencies:
  core:
    - contracts
    - core-bus
    - plugin-host
  optional:
    - math-kernel
    - hal-rs

profiles:
  default: profiles/hft-core.yaml
